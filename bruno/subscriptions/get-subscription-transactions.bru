meta {
  name: Get Subscription Transactions
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/user/subscriptions/{{subscriptionId}}/transactions?page={{page}}&limit={{limit}}&status={{transactionStatus}}&fromDate={{fromDate}}&toDate={{toDate}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
