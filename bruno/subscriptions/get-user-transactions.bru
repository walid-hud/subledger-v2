meta {
  name: Get User Transactions
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/user/transactions?page={{page}}&limit={{limit}}&status={{transactionStatus}}&subscriptionId={{subscriptionId}}&fromDate={{fromDate}}&toDate={{toDate}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
